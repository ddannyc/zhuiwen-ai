# 复制为 .env 并按本机调整。.env 不提交。
#
# 默认指向 compose 起的容器（docker compose up -d）：宿主端口 5433，避让本机原生 pg。
# init 脚本已在容器内造好 app 角色 + vector 扩展（docker/postgres/init/01-init.sql）。
# 业务连接：受 RLS 约束的 app 角色（运行期用）
database_url=postgresql+asyncpg://app:app@localhost:5433/xborder
# 管理连接：超级用户（建库/角色/扩展/迁移用）。容器超管 = postgres/postgres。
database_admin_url=postgresql+asyncpg://postgres:postgres@localhost:5433/xborder
# 若改用本机原生 pg：端口换 5432，超管按本机配置（Homebrew/Postgres.app 常为 OS 用户名+trust）。

# JWT（生产务必换成 >=32 字节随机串）
# jwt_secret=change-me

# CORS：前端允许源。生产把 CORS_ORIGINS 设为真实前端域名（CSV），并清空 CORS_ORIGIN_REGEX
# （私网正则仅开发便利，生产不放行局域网）。开发默认正则已覆盖 localhost + 私网 IP。
# CORS_ORIGINS=https://seller.example.com
# CORS_ORIGIN_REGEX=

# chat LLM —— gateway 用 LiteLLM SDK 进程内调用，打阿里百炼 (DashScope) 兼容端点。
# 国内区（中国大陆，百炼中国站 Key）。国际区改 dashscope-intl.aliyuncs.com（且需新加坡站 Key，不通用）。
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
DASHSCOPE_API_KEY=<百炼中国站控制台 Key>
# chat_model=qwen-plus              # 默认模型

# embeddings —— 与 chat 同源：经 LiteLLM SDK 进程内调 DashScope（复用上面的 DASHSCOPE_*）。
# knowledge_base + rules_kb 域共用。默认 text-embedding-v3 / 1024 维（对齐 kb_chunks）。
# EMBEDDING_MODEL=text-embedding-v3
# EMBEDDING_DIM=1024
